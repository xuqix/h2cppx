TODO
====

待完善功能
==========

master:

* 提供后缀设置选项(可选),以支持不同后缀的c/cpp文件(*.c *.cc *.cxx *.cpp) done
* 提供实现文件搜索目录设置选项(可选)(默认为.h所在目录) done
* 若搜索实现文件失败,将统一在何目录生成的选项(可选)(默认使用所搜索的目录) done


vim-port:

* 提供路径搜索配置 done

